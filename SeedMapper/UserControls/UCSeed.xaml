<UserControl
	x:Name="ucSeed"
	x:Class="SeedMapper.UserControls.UCSeed"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="clr-namespace:SeedMapper.UserControls"
	mc:Ignorable="d"
	d:DesignHeight="300" d:DesignWidth="300">

	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition />
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>

		<Button Grid.Column="0" Content="{Binding Path=LimsId}" Width="100" />
		<Button Grid.Column="1" Content="{Binding Path=GroupNumber}" Width="50" />
		<Button Grid.Column="2" Content="{Binding Path=GroupSequence}" Width="50" />
		<Button Grid.Column="3" Content="{Binding Path=DestinationContainer}" Width="200" />
		<Button Grid.Column="4" Content="{Binding Path=DestinationWell}" Width="50" />
	</Grid>
</UserControl>