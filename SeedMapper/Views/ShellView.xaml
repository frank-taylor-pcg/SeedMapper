<Window
	x:Class="SeedMapper.Views.ShellView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="clr-namespace:SeedMapper.Views"
	xmlns:userControls="clr-namespace:SeedMapper.UserControls"
	mc:Ignorable="d"
	Background="DimGray"
	Title="ShellView" Height="1080" Width="1920">
	
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition  />
			<ColumnDefinition  />
		</Grid.ColumnDefinitions>
		
		<ItemsControl Grid.Column="0" Background="LightGray" MinWidth="300" x:Name="OPlates">
			<!-- Host panel template -->
			<ItemsControl.ItemsPanel>
				<ItemsPanelTemplate>
					<UniformGrid Columns="2" Rows="3" />
				</ItemsPanelTemplate>
			</ItemsControl.ItemsPanel>
			
			<!-- Item template -->
			<ItemsControl.ItemTemplate>
				<DataTemplate>
					<userControls:UCOplate Barcode="{Binding Barcode}" Seeds="{Binding Seeds}" />
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ItemsControl>
		
		<ListView Grid.Column="1" x:Name="Seeds">
			<ListView.ItemTemplate>
				<DataTemplate>
					<userControls:UCSeed />
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
	</Grid>
</Window>